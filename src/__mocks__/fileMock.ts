/**
 * File Mock
 *
 * This mock is used to handle static file imports (images, fonts, etc.) in tests.
 * When Jest encounters an import for a file with extensions like .jpg, .png, .svg, etc.,
 * it will use this mock instead of trying to process the actual file.
 */

export default 'test-file-stub'
